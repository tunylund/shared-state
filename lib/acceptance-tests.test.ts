import { GAMESTATE } from "./gamestate"

describe('high-level-tests', () => {
  const initialState = {some: 'state'}

  function buildClient() {}

  function buildServer() {}

  it('should expect all interation tests to have been done :/', () => {
    // buildServer()
    // const {on} = buildClient()
    // expect(on).toHaveBeenCalledWith(GAMESTATE.INIT, initialState)
  })

})
